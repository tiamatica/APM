 ReadConfig←{
     cfg_file←⍵,'/',CFG_NAME
     ~⎕NEXISTS cfg_file:1 ''⍬
     cfg←#.⎕NS''
     ⍝ set up defaults
     cfg.source←''
     cfg.dependencies←⍬
     ⍝ load in actual
     _←'cfg'⎕NS ⎕JSON⊃⎕NGET cfg_file
     0 ''cfg
 }
