 InitPackage←{
     ⍺←⎕NS''
     path←⊃1 ⎕NPARTS ⍵,'/'
     cfg_file←path,CFG_NAME
     ⎕NEXISTS cfg_file:1 'Package definition already exists'⍬
     cfg←InitPackageConfig''
     _←'cfg'⍺.⎕NS(⍺.⎕NL-2)∩cfg.⎕NL-2
     cfg WriteConfig path
 }
