 test_serverless_registry_load_package←{
     apm flags←⍺ ⍵
     dir←getTempDir ⍬
     rc registry←apm CreateTestRegistry dir
     assert rc=0:

     uri←apm.PathToUri registry
     rc msg cfg←apm.AddRegistry'Firlefanz'uri
     assert rc=0:

     rc msg cfg←apm.Load'Example-Test-0.1.0'
     assert 0=rc:

     rc msg cfg←apm.RemoveRegistry'Firlefanz'
     assert rc=0:

     _←3 ⎕NDELETE dir
     0
 }
