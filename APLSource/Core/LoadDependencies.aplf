 LoadDependencies←{
     cfg←⍵
     0=≢cfg.(dependencies,devDependencies):0
     ns←⍎cfg.nspath ⎕NS''
     rc msg←↓⍉↑ns{
         rc msg dc←Load ⍵
         rc≠0:rc('Loading dependency ',⍵,' failed: ',msg)
         _←⍺⍎dc.(name,'←',nspath)
         0 ''
     }¨cfg.(dependencies,devDependencies)
     rc∧.=0:0 ''
     1(⊃{'Error loading ',(3⊃⍵),', rc=',(⍕1⊃⍵)'; ',2⊃⍵}/(0≠rc)∘/¨rc msg cfg.dependencies)
 }
