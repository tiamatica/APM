 List←{
     regName←⍺
     rc msg cfg←ReadSettings''
     0≠rc:rc msg
     registry←cfg.registries⊃⍨cfg.registries.alias⍳⊂regName
     res←registry{
         ⊃'file://'⍷⍺.uri:⍺ ListViaFile ⍵
         {≢'^http[s]*://'⎕S 0⊣⍵}⍺:⍺ ListViaHTTP ⍵
     }¨⊂⍵
     rcs←⊃¨res
     packs←⊃,/(rcs=0)/2⊃¨res
     0 ''packs
 }
