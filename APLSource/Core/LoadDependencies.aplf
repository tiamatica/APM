 LoadDependencies←{
     cfg←⍵
     0=≢cfg.dependencies:0
     ns←⍎cfg.nspath ⎕NS''
     _←ns{
         rc msg dc←Load ⍵
         rc≠0:∘∘∘   ⍝TODO⍝
         ⍺⍎dc.(name,'←',nspath)
     }¨cfg.dependencies
     0
 }
