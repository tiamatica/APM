 test_add_registry←{
     apm flags←⍺ ⍵
     rc msg cfg←apm.ReadSettings GetTestRegFilename
     assert rc=0:
     assert 0=≢cfg.registries:

     uri←'https://apl.registry.com'
     rc msg cfg←GetTestRegFilename apm.AddRegistry'MyAlias'uri
     assert rc=0:

     rc msg cfg←apm.ReadSettings GetTestRegFilename
     assert rc=0:
     assert uri≡⊃,/cfg.registries.uri:

     0
 }
