 RunAll←{
     ⍺←##.Core
     apm←⍺
     debug←flags←⍵
     new_cfg←(getTempDir''),'/apm_config.json'
     old_cfg←apm.SetGlobalConfigFile new_cfg
     fns←⎕NL-3
     tests←fns/⍨⊣/'test_'⍷↑fns
     rc←⍺{
         (0/⍨debug=0)::1
         ⍺(⍎⍵)flags  ⍝ execute test
     }¨tests
     _←apm.SetGlobalConfigFile old_cfg
     'Passed (',(⍕+/rc=0),'/',(⍕≢tests),') tests'
 }
