 test_serverless_registry_load_package←{
     apm flags←⍺ ⍵
     dir←tempDir ⍬
     rc registry←apm CreateTestRegistry dir
     assert rc=0:

     uri←apm.PathToUri registry
     rc msg←apm.AddRegistry uri
     assert rc=0:

     rc msg←apm.Load'Example-Test-0.1.0'

     rc msg←apm.RemoveRegistry uri
     assert rc=0:

     _←3 ⎕NDELETE dir
     0
 }
