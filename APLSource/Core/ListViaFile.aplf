 ListViaFile←{
     uri pattern←⍺ ⍵
     type←'file://'
     0=⊃type⍷uri:1 'Only file URI supported.'

     path←(≢type)↓uri
     rc ind←GetRegistryIndex path
     0≠rc:rc ind
     mask←∨/pattern⍷⍕↑ind.(group,'-',name,'-v',version)
     pkgs←mask/ind
     0 pkgs
 }
