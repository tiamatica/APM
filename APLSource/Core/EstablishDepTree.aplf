 EstablishDepTree←{
     ⍝ Resolve dependency tree and save it in project folder ⍵
     ⍺←'exact'
     mode←⍺
     prj_dir←⊃1 ⎕NPARTS ⍵,'/'
     rc msg cfg←ReadConfig prj_dir
     tree←mode ResolveDepTree cfg.dependencies
     tree WriteDepTree prj_dir
 }
