 LoadSource←{
     ⍝ Load code from dir ⍵
     ⍝ include dependencies if ⍺=1 (default 0)
     ⍺←0
     doDep←⍺
     dir←⍵
     rc msg cfg←ReadConfig dir
     rc≠0:1 msg ⍬

     cfg.nspath←PackageSpacePath cfg
     9=⎕NC cfg.nspath:0 ''cfg

     (rc msg)←LoadDependencies⍣doDep⊢cfg
     ⍝TODO⍝ rc?!

     src←dir,'/',cfg.source
     msg←⎕SE.Link.Import cfg.nspath src   ⍝ "msg" might be useful when tracing
     0 ''cfg
 }
