 RunAll←{
     ⍺←##.Core
     debug←flags←⍵
     fns←⎕NL-3
     tests←fns/⍨⊣/'test_'⍷↑fns
     _←3 ⎕NDELETE GetTestRegPath
     rc←3 ⎕MKDIR GetTestRegPath
     assert 1=rc:
     rc←⍺{
         (0/⍨debug=0)::1
         ⍺(⍎⍵)flags  ⍝ execute test
     }¨tests
     rc←3 ⎕MKDIR GetTestRegPath
     'Passed (',(⍕+/rc=0),'/',(⍕≢tests),') tests'
 }
