 ParsePackageName←{
     ⍝ ⍵ ←→ [Group-]Name[-Version]
     ⍝ ← ←→ group name version
     parts←'-'(≠⊆⊢)⍵
     mask←{∧/⍵∊⎕D,'.'}¨parts
     g n←¯2↑(⊂''),parts/⍨∧\~mask
     v←1↓∊'-',¨parts/⍨∨\mask
     g n v
 }
